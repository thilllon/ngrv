{
  "name": "example",
  "version": "4.0.0",
  "private": true,
  "scripts": {
    "build:metadata": "ngrv generate --cwd . --output build-info.json --strict",
    "start": "node src/index.mjs",
    "test": "pnpm build:metadata && pnpm start"
  },
  "license": "MIT",
  "dependencies": {
    "@opentelemetry/api": "1.9.0",
    "@opentelemetry/resources": "2.11.0",
    "@opentelemetry/sdk-node": "0.222.0",
    "ngrv": "link:.."
  }
}
